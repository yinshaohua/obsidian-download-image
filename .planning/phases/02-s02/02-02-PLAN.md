---
phase: "02"
plan: "02"
type: "implementation"
wave: 2
depends_on:
  - "02-01"
---

<objective>
Write maintainer submission checklist with known blockers and community-plugins.json entry
</objective>

<context>
Phase: 02 (s02) — Milestone: Submit plugin to Obsidian community catalog

Create `SUBMISSION_CHECKLIST.md` at the repo root — a maintainer-facing document that walks through every step needed to validate release assets, create the GitHub release, and open/update the `obsidian-releases` pull request. The checklist must surface known blockers (plugin ID format, empty authorUrl, missing release workflow) and include the exact `community-plugins.json` entry shape.

This is the documentation half of the slice deliverable. It references the verification script from T01 as the automated preflight step.
</context>

<must_haves>

- Checklist references `npm run verify-release` as the preflight step
- Known blockers section documents plugin ID, empty authorUrl, and missing release workflow
- Exact `community-plugins.json` entry shape is included
- Release creation steps specify bare semver tag (no `v` prefix) and list all three release assets
- D001 decision boundary is explicitly stated
- No TBD or TODO placeholders remain

</must_haves>
